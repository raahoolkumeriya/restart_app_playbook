---
language: python
python: "2.7"

before_install:
  # Make sure everything's up to date.
  - sudo apt-get update -qq

install:
  # Install Ansible.
  - pip install ansible

  - echo 'SHA256:cij4zTaY4lS9hX05L6vu0CbYfdXTNT00/p2lmR0IbhU' >> $HOME/.ssh/known_hosts 
  
script:
  - ansible-playbook -i application_restart/inventory application_restart/restart.yml  
